version: "3.8"
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      target: production
    container_name: integral_laravel_prod_app
    restart: always
    working_dir: /var/www
    volumes:
      - storage_data:/var/www/storage
      - app_public:/var/www/public
    env_file:
      - .env
    environment:
      APP_ENV: production
      APP_DEBUG: "false"
    networks:
      - dokploy-network
  web:
    image: nginx:alpine
    container_name: integral_nginx_prod_front
    restart: always
    expose:
      - "80"
    volumes:
      - app_public:/var/www/public:ro
      - ./docker/nginx/prod.conf/default.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - app
    networks:
      - dokploy-network
    labels:
      - traefik.docker.network=dokploy-network
      - traefik.http.routers.integral-front-03exyr-40-web.rule=Host(`integraldemo.webinfo.sv`)
      - traefik.http.routers.integral-front-03exyr-40-web.entrypoints=web
      - traefik.http.services.integral-front-03exyr-40-web.loadbalancer.server.port=80
      - traefik.http.routers.integral-front-03exyr-40-web.service=integral-front-03exyr-40-web
      - traefik.http.routers.integral-front-03exyr-40-web.middlewares=redirect-to-https@file
      - traefik.http.routers.integral-front-03exyr-40-websecure.rule=Host(`integraldemo.webinfo.sv`)
      - traefik.http.routers.integral-front-03exyr-40-websecure.entrypoints=websecure
      - traefik.http.services.integral-front-03exyr-40-websecure.loadbalancer.server.port=80
      - traefik.http.routers.integral-front-03exyr-40-websecure.service=integral-front-03exyr-40-websecure
      - traefik.http.routers.integral-front-03exyr-40-websecure.tls.certresolver=letsencrypt
      - traefik.enable=true
networks:
  dokploy-network:
    external: true
volumes:
  storage_data: null
  app_public: null